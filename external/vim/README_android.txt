README for version 7.4 of Vim: Vi IMproved.

If vim (or nano) doesnt use the whole screen in adb shell,
just execute "busybox resize" before vim execution

you can also export env variables LINES and COLUMNS to set
vi to a specific size

the "resize" command could be added to /system/etc/mkshrc

